server:
  port: 7001
eureka:
  instance:
    hostname: localhost #eureka服务器端实例名称
  client:
    register-with-eureka: false #false表示自己不向自己注册
    fetch-registry: false  #false表示自己就是注册中心
    service-url:
     defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka/
